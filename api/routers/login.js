const Router = require('koa-router');

// const db = require('../db');

// 创建路由
var router = new Router();

router.get('/',(ctx,next)=>{

    // //登录
    // console.log(ctx.respond)

})

module.exports = router;